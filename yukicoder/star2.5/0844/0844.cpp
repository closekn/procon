#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, a;
  cin >> n >> m >> a;
  int l[m], r[m], p[m];
  for ( int i = 0; i < n; i++ ) { cin >> l[i] >> r[i] >> p[i]; }

  return 0;
}