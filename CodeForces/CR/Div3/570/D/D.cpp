#include <bits/stdc++.h>
using namespace std;

int main() {
  int que;
  cin >> que;
  
  for ( q = 0; q < que; q++ ) {
    int n;
    cin >> n;
    int a[n];
    for ( int i = 0; i < n; i++ ) { cin >> a[i]; }

  }

  return 0;
}