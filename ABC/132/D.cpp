#include <bits/stdc++.h>
using namespace std;

long long conbination(int l, int r) {

}

int main() {
  int n, k;
  cin >> n >> k;
  int red = n-k;
  long long all = conbination(k+1, );
  long long ans[k+1];
  ans[1] = n-k+1;
  for ( int i = 2; i <= k; i++ ) {
    int sif[i+2];
    
  }

  return 0;
}